
INFO
elements_of_dropDown (array)
  text
  path_to_image
  function

preSelected_option
--------------------------


function buildDropDown(elements_of_dropDown, preSelected_option)

  create div dropDownHTML

  loop through elements_of_dropDown
    append dropDownElement(oneElement) to dropDownHTML

  return dropDownHTML


function dropDownElement(oneElement)
  create div elementHTML
  set text
  set image 
  set eventlistener to function
  return elementHTML

const countries = countries.map
append buildDropDown(countries, "Sverige" or id for the country) to body


INFO
search_on_cities
---------------------------

function searchOnCities()
  return the input value toLowerCase()


function citiesResult ()
  cityArray = []

  loop through the cities
    get the search element from HTML = "";

    if("" == searchOnCities())
      get the search element from HTML = "";
    else if (cities[i].name toLowerCase() includes searchOnCities())
      cityArray.push(the info needed)

  renderCities(cityArray)


INFO
render_cities_on_destination_page 
----------------------------------